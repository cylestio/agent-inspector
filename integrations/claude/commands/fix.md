---
argument-hint: [REC-XXX]
description: Fix a security recommendation
---

**FIRST: You MUST run the agent-inspector-setup skill (Preflight Check section).** Do not skip this.

THEN run the agent-inspector-fix skill to fix recommendation $ARGUMENTS

If no argument provided, fix the highest priority blocking issue.
